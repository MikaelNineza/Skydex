ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.skydex.server.ApplicationKt.module ]
    }
}

# Secrets come from environment variables. Never commit real values.
hypixel {
    apiKey = ""
    apiKey = ${?HYPIXEL_API_KEY}
}

database {
    url = "jdbc:postgresql://localhost:5432/skydex"
    url = ${?DATABASE_URL}
}
