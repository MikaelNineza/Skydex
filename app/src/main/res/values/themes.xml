<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark window background so there is no white flash before Compose draws. -->
    <style name="Theme.Skydex" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/skydex_background</item>
    </style>
</resources>
